class Brain {
    constructor(inputs, hidden, outputs) {
        this.inputsLayers = createLayer(inputs);
        this.hidden = hidden;
        this.outputs = outputs;
    }

    createLayer (nodesNumber) {
        
    }

    init() {

    }
}